apiVersion: v1
kind: Service
metadata:
  name: llm-monkey
  labels:
    app: llm-monkey
spec:
  selector:
    app: llm-monkey
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http
  type: ClusterIP
