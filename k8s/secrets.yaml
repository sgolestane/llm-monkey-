apiVersion: v1
kind: Secret
metadata:
  name: llm-monkey-secrets
  labels:
    app: llm-monkey
type: Opaque
stringData:
  # Replace these values before applying.
  # In production, use External Secrets Operator or Sealed Secrets instead of this file.
  SPRING_DATASOURCE_USERNAME: "llmmonkey"
  SPRING_DATASOURCE_PASSWORD: "CHANGE_ME"
  LLM_MONKEY_MASTER_KEY: "CHANGE_ME"
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
